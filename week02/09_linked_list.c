#include <stdio.h> 
#include <stdlib.h>


typedef struct Node {
    char data;
    struct Node* next_p;
} Node_t;

int main() {

    Node_t* head_p; 
    head_p = (Node_t*)malloc(sizeof(Node_t));

}

